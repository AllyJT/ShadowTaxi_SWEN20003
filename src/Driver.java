import java.util.Properties;

public class Driver extends Entity implements  Invincible{

    public Driver(String string, double x, double y, double radius) {
        super(string, x, y, radius);
        setVisible(false);
    }

}
